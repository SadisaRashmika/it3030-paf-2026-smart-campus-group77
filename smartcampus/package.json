{
  "name": "smartcampus-backend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "mvnw.cmd spring-boot:run",
    "build": "mvnw.cmd clean package",
    "test": "mvnw.cmd test"
  }
}
